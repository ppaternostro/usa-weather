import { Geocode } from '../../../model/geocode/geocode.model';
import { invalidMockResult } from './invalid-mock-result';

export const invalidMockGeocode: Geocode = {
  result: invalidMockResult,
};
