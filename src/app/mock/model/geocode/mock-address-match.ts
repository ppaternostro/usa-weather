import { AddressMatch } from '../../../model/geocode/address-match.model';
import { mockCoordinate } from './mock-coordinate';

export const mockAddressMatch: AddressMatch = {
  coordinates: mockCoordinate,
  matchedAddress: '1535 Broadway, New York, NY, 10036',
};
